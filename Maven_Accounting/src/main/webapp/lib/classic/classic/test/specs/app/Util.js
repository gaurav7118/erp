describe("Ext.app.Util",function(){it("has namespaces property",function(){expect(Ext.app.namespaces).toBeDefined()});it("adds single namespace to known list",function(){Ext.app.addNamespaces("Foo");expect(Ext.app.namespaces["Foo"]).toBeTruthy()});it("adds multiple namespaces to known list",function(){Ext.app.addNamespaces(["Foo.bar","Foo.bar.baz","Qux"]);var A=Ext.app.namespaces;expect(A["Foo.bar"]).toBeTruthy();expect(A["Foo.bar.baz"]).toBeTruthy();expect(A["Qux"]).toBeTruthy()});it("resolves namespace for a class",function(){var A=Ext.app.getNamespace("Foo.Bar");expect(A).toBe("Foo")});it("resolves nested namespace for a class",function(){var A=Ext.app.getNamespace("Foo.bar.Baz");expect(A).toBe("Foo.bar")});it("resolves even deeper nested namespace for a class",function(){var A=Ext.app.getNamespace("Foo.bar.baz.Qux");expect(A).toBe("Foo.bar.baz")});it("returns undefined when class belongs to unknown namespace",function(){var A=Ext.app.getNamespace("foo.bar.baz.Qux");expect(A).toBeUndefined()});it("clears namespaces",function(){Ext.app.clearNamespaces();var A=Ext.Object.getKeys(Ext.app.namespaces);expect(A).toEqual([])})})