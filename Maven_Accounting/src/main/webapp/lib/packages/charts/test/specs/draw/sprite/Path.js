describe("Ext.draw.sprite.Path",function(){describe("hitTest",function(){var C,A,B;beforeEach(function(){B=new Ext.draw.Container();A=new Ext.draw.Surface();C=new Ext.draw.sprite.Circle({hidden:false,globalAlpha:1,fillOpacity:1,strokeOpacity:1,fillStyle:"red",strokeStyle:"red",r:100,cx:100,cy:100});A.add(C);B.add(A)});afterEach(function(){Ext.destroy(C,A,B)});it("should return an object with the 'sprite' property set to the sprite itself, if the sprite is visible and its bounding box and path are hit",function(){var D=C.hitTest([90,90]);expect(D&&D.sprite).toBe(C)});it("should return null, if the sprite is visible, its bounding box is hit, but the path isn't",function(){var D=C.hitTest([10,10]);expect(D).toBe(null)})})})