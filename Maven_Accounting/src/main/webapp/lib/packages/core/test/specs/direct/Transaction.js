describe("Ext.direct.Transaction",function(){var B,A;beforeEach(function(){A={queueTransaction:jasmine.createSpy("provider.queueTransaction")};B=new Ext.direct.Transaction({provider:A})});it("can instantiate",function(){expect(B).toBeDefined()});it("increments transaction id",function(){expect(B.tid).toBeGreaterThan(0)});it("sends",function(){B.send();expect(A.queueTransaction).toHaveBeenCalled()});it("retries",function(){B.retry();expect(A.queueTransaction).toHaveBeenCalled();expect(B.retryCount).toBe(1)});it("returns provider",function(){var C=B.getProvider();expect(C).toEqual(A)})})